import React from 'react';
import { Container } from '../../components/Container';

/**
 * TODO:
 * - Add tailwind.css
 * - Add hook form
 * - Add jwt token decode
 * - Add toast
 * - Add storybook
 * - Add tests
 *
 * Maybe:
 * - Add animation lib: animate.css / framer-motion
 * - Add GraphQL
 *
 * + Add react icons
 */

export const Home = () => {
    return (
        <div>
            <Container>
                <h1>Welcome visitor!</h1>
            </Container>
        </div>
    )
}
